name: bbk_ai
description: Minimal Flutter starter that builds on GitHub Actions.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # (أتركها فاضية الآن بدون حزم طرف ثالث عشان البناء ينجح 100%)
  # لما نبي نضيف OCR لاحقًا، نرجع نحدّثها.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    # - assets/sample.png
