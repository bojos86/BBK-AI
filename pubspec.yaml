name: bbk_ai_app
description: BBK AI Mobile Banking
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.2
  intl: ^0.19.0
  path_provider: ^2.1.4

  # OCR (Google ML Kit)
  google_mlkit_text_recognition: ^0.10.0

  # الكاميرا — إصدار متوافق مع Flutter الحالي
  camera: ^0.10.5+9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/app_banner.png
    - assets/app_icon.png
    - assets/splash_logo.png
